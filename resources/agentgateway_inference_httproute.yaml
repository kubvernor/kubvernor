apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: agw-llm-route
spec:
  parentRefs:
    - name: agentgateway-one
      group: gateway.networking.k8s.io
      kind: Gateway
  hostnames:
    - "www.inference-one.com"
  rules:
    - backendRefs:
        - group: inference.networking.x-k8s.io
          kind: InferencePool
          name: vllm-llama3-8b-instruct
      matches:
        - path:
            type: PathPrefix
            value: /inference
